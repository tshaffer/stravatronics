{
  "name": "stravatronics",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.31.0",
  "scripts": {
    "dev:web": "pnpm --filter @stravatronics/web dev",
    "dev:api": "pnpm --filter @stravatronics/api dev",
    "dev": "concurrently \"pnpm:dev:web\" \"pnpm:dev:api\"",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "concurrently": "9.2.1",
    "rimraf": "6.1.3",
    "typescript": "5.9.3"
  }
}
